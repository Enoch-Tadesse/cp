#include <bits/stdc++.h>
using namespace std;

void solve() { cout << "Hello world"; }
int main() {

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }
}
